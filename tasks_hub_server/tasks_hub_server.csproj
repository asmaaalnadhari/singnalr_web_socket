<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- Target .NET 7.0 (you can bump to 8.0 or later if you have that SDK) -->
    <TargetFramework>net7.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- SignalR is included in the ASP.NET Core shared framework, so no extra PackageReference is required. -->
</Project>
